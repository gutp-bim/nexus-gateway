<?xml version="1.0" encoding="UTF-8"?>
<!--
  Structured JSON logging for the OPC-UA connector (issue #36).
  Emits exactly one JSON object per line with the shared key set:
    timestamp, level, connector_id (via MDC), message
  plus a stackTrace field only when an exception is attached.
  The key set is shared across all connectors — do not rename or add @-prefixes.
-->
<configuration>
  <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
    <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
      <providers>
        <timestamp>
          <fieldName>timestamp</fieldName>
          <timeZone>UTC</timeZone>
          <pattern>yyyy-MM-dd'T'HH:mm:ss.SSSXXX</pattern>
        </timestamp>
        <logLevel>
          <fieldName>level</fieldName>
        </logLevel>
        <mdc/>
        <message>
          <fieldName>message</fieldName>
        </message>
        <stackTrace/>
      </providers>
    </encoder>
  </appender>

  <root level="INFO">
    <appender-ref ref="JSON"/>
  </root>
</configuration>
